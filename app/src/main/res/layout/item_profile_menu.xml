<?xml version="1.0" encoding="utf-8"?>
<!--
    item_profile_menu.xml
    Custom Layout reusable for menu items in Profile
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <!-- Circle Icon Frame -->
    <FrameLayout
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_menu_icon_circle">

        <ImageView
            android:id="@+id/menu_icon"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@android:drawable/ic_menu_preferences"
            app:tint="@color/primary"
            android:layout_gravity="center"
            android:contentDescription="Menu icon" />
    </FrameLayout>

    <!-- Nhóm text: title + subtitle -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/menu_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tên mục"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/color_text_primary" />

        <TextView
            android:id="@+id/menu_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="12sp"
            android:textColor="@color/color_text_secondary"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Mũi tên phải -->
    <ImageView
        android:id="@+id/menu_arrow"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@android:drawable/ic_media_next"
        app:tint="@color/color_inactive_icon"
        android:contentDescription="" />

</LinearLayout>