<?xml version="1.0" encoding="utf-8"?>
<!-- item_wallet_transaction.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="14dp"
    android:background="?attr/selectableItemBackground">

    <!-- Icon vòng tròn -->
    <FrameLayout
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginEnd="14dp">

        <TextView
            android:id="@+id/tv_tx_icon_bg"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="#F0F0F8"
            android:gravity="center" />

        <TextView
            android:id="@+id/tv_tx_icon"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:gravity="center"
            android:text="🪙"
            android:textSize="20sp" />
    </FrameLayout>

    <!-- Nội dung giữa -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_tx_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Giao dịch"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#1A1A2E" />

        <TextView
            android:id="@+id/tv_tx_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="12sp"
            android:textColor="#888888"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />

        <TextView
            android:id="@+id/tv_tx_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="11sp"
            android:textColor="#BBBBBB"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Số coin bên phải -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="end">

        <TextView
            android:id="@+id/tv_tx_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+0 🪙"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#10B981" />

        <!-- Badge trạng thái (cho rút tiền) -->
        <TextView
            android:id="@+id/tv_tx_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="10sp"
            android:textColor="#888"
            android:layout_marginTop="3dp"
            android:gravity="end"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Separator dưới (vẽ bằng background divider) -->
</LinearLayout>
